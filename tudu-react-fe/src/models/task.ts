export type Task = {
    id: string,
    taskName: string,
    taskDescription: string,
    done: boolean
}

export type TaskReturnModel = {
    sk: string,
    taskName: string,
    taskDescription: string,
    done: boolean
}